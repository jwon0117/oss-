| 번호 | 유형 | 정답 | 문제 | 보기1 | 보기2 | 보기3 | 보기4 | 보기5 |
|------|------|------|------|-------|-------|-------|-------|-------|
| 1    | 1    | 귀도 반 로섬 | 파이썬 제작자의 이름은? | 폰 노이만 | 웰터 피츠 | 귀도 반 로섬 | 앨런 튜링 | 데니스 리치 |
| 2    | 2    | 리스트 | 다양한 자료형의 집합이고 수정 가능한 객체는? (3글자, 한글로) | | | | | |
| 3    | 3    | X    | C언어에서 TensorFlow를 사용할 수 있다. | | | | | |
| 4    | 1    | `print()` | 파이썬에서 값을 출력하는 함수는 무엇인가? | `input()` | `print()` | `output()` | `echo()` | `display()` |
| 5    | 2    | `True` | 파이썬에서 `2 > 1`의 결과는 무엇인가? | `False` | `True` | `None` | `0` | `1` |
| 6    | 3    | O    | 파이썬에서 `a = 5`와 `b = 5`일 때, `a is b`는 True이다. | | | | | |
| 7    | 1    | `int` | `input()` 함수로 받은 값은 기본적으로 어떤 자료형으로 반환되는가? | `str` | `int` | `float` | `list` | `bool` |
| 8    | 2    | `[]` | 빈 리스트를 나타내는 표기법은 무엇인가? | `()` | `[]` | `{}` | `""` | `None` |
| 9    | 1    | `float` | 5를 2로 나눈 결과는 어떤 자료형인가? | `int` | `float` | `str` | `list` | `bool` |
| 10   | 3    | O    | 파이썬에서 `None`은 값을 할당하지 않은 상태를 의미한다. | | | | | |
| 11   | 1    | `and` | 파이썬에서 두 조건이 모두 참일 때 사용하는 논리 연산자는 무엇인가? | `or` | `and` | `not` | `xor` | `&&` |
| 12   | 1    | `len()` | 파이썬에서 문자열이나 리스트의 길이를 구하는 함수는 무엇인가? | `count()` | `len()` | `size()` | `length()` | `strlen()` |
| 13   | 3    | X    | 파이썬에서 문자열은 변경 가능한 객체이다. | | | | | |
| 14   | 1    | `in` | 파이썬에서 특정 값이 리스트나 문자열에 포함되어 있는지 확인할 때 사용하는 연산자는 무엇인가? | `is` | `in` | `==` | `contains` | `has` |
| 15   | 2    | `3` | `print("Hello"[2])`의 결과는 무엇인가? | | | | | |
| 16   | 1    | `False` | `a = [1, 2, 3]; a == [1, 2, 3]`의 결과는 무엇인가? | `True` | `False` | `None` | `[]` | `1` |
| 17   | 3    | O    | 파이썬의 튜플은 변경 불가능한 자료형이다. | | | | | |
| 18   | 2    | `1` | `10 % 3`의 결과는 무엇인가? | | | | | |
| 19   | 1    | `range()` | 0부터 9까지의 숫자를 생성하는 함수는 무엇인가? | `range()` | `list()` | `tuple()` | `set()` | `dict()` |
| 20   | 3    | O    | 파이썬에서는 리스트와 튜플을 인덱스로 접근할 수 있다. | | | | | |
| 21   | 2    | `a = [1, 2, 3]; a.append(4)` | 리스트에 값을 추가하는 메서드는 무엇인가? | | | | | |
| 22   | 1    | `set()` | 중복을 제거하고 고유한 값만 저장할 수 있는 자료형은 무엇인가? | `list` | `set` | `dict` | `tuple` | `frozenset` |
| 23   | 1    | `zip()` | 두 개 이상의 리스트를 묶어서 튜플 형태로 반환하는 함수는 무엇인가? | `combine()` | `merge()` | `zip()` | `join()` | `map()` |
| 24   | 3    | O    | 파이썬에서 `x = None`은 값이 할당되지 않은 상태를 의미한다. | | | | | |
| 25   | 2    | `4` | `4 // 2`의 결과는 무엇인가? | | | | | |
| 26   | 3    | X    | 파이썬의 리스트는 크기가 고정된 자료형이다. | | | | | |
| 27   | 1    | `class` | 파이썬에서 클래스를 정의할 때 사용하는 키워드는 무엇인가? | `def` | `class` | `object` | `function` | `type` |
| 28   | 2    | `str` | `'123'`을 숫자로 변환하려면 어떤 자료형으로 변환해야 하는가? | `str` | `int` | `float` | `bool` | `list` |
| 29   | 1    | `+=` | 파이썬에서 변수에 값을 더할 때 사용하는 연산자는 무엇인가? | `+` | `++` | `+=` | `add` | `append` |
| 30   | 2    | `8` | `2 ** 3`의 결과는 무엇인가? | | | | | |
| 31   | 1    | `sorted()` | 파이썬에서 리스트를 정렬하는 함수는 무엇인가? | `sort()` | `order()` | `sorted()` | `arrange()` | `reverse()` |
| 32   | 2    | `3` | `list(range(3))`은 무엇을 반환하는가? | | | | | |
| 33   | 3    | O    | 파이썬에서 `is` 연산자는 객체의 아이디를 비교한다. | | | | | |
| 34   | 1    | `False` | `a = [1, 2]; b = [1, 2]; a == b`의 결과는 무엇인가? | `True` | `False` | `None` | `[]` | `1` |
| 35   | 2    | `True` | `bool(0)`의 결과는 무엇인가? | `False` | `True` | `None` | `0` | `1` |
| 36   | 1    | `break` | 파이썬에서 반복문을 종료하는 명령어는 무엇인가? | `exit` | `stop` | `break` | `return` | `continue` |
| 37   | 3    | O    | 파이썬에서 `[]`는 빈 리스트를 의미한다. | | | | | |
| 38   | 1    | `isinstance()` | 특정 객체가 특정 자료형인지 확인하는 함수는 무엇인가? | `type()` | `instance()` | `isinstance()` | `checktype()` | `verify()` |
| 39   | 1    | `else` | `if` 문에서 조건이 거짓일 때 실행되는 코드를 작성하는 키워드는 무엇인가? | `then` | `otherwise` | `else` | `elif` | `end` |
| 40   | 2    | `4` | `print(2 + 2 / 2)`의 결과는 무엇인가? | | | | | |
| 41   | 1    | `def` | 파이썬에서 함수를 정의할 때 사용하는 키워드는 무엇인가? | `define` | `def` | `function` | `func` | `lambda` |
| 42   | 3    | X    | 파이썬에서 문자열은 수정 가능한 객체이다. | | | | | |
| 43   | 1    | `*` | 파이썬에서 두 숫자를 곱할 때 사용하는 연산자는 무엇인가? | `+` | `-` | `*` | `/` | `%` |
| 44   | 2    | `1` | `print(2**3 % 3)`의 결과는 무엇인가? | | | | | |
| 45   | 3    | O    | 파이썬에서 `==`는 두 값이 같음을 비교하는 연산자이다. | | | | | |
| 46   | 1    | `map()` | 파이썬에서 함수와 iterable을 결합하는 함수는 무엇인가? | `filter()` | `map()` | `reduce()` | `zip()` | `join()` |
| 47   | 2    | `None` | `x = "hello"; del x` 후에 `x`의 값은 무엇인가? | | | | | |
| 48   | 3    | O    | 파이썬에서 `for` 문은 시퀀스를 순차적으로 반복한다. | | | | | |
| 49   | 1    | `append()` | 리스트에 값을 추가하는 메서드는 무엇인가? | `insert()` | `append()` | `extend()` | `add()` | `push()` |
| 50   | 2    | `True` | `x = 5; y = 3; x > y`의 결과는 무엇인가? | | | | | |
| 51   | 3    | X    | 파이썬에서 `is` 연산자는 두 객체의 값을 비교한다. | | | | | |
| 52   | 1    | `__init__()` | 파이썬에서 클래스의 생성자 메서드는 무엇인가? | `__init__()` | `__new__()` | `__call__()` | `__construct__()` | `__start__()` |
| 53   | 3    | O    | 파이썬에서는 `x == 5`와 `x is 5`는 동일한 결과를 반환할 수 있다. | | | | | |
| 54   | 2    | `5` | `x = [1, 2, 3, 4, 5]; x[4]`의 결과는 무엇인가? | | | | | |
| 55   | 1    | `break` | 파이썬에서 무한 루프를 종료하는 방법은 무엇인가? | `exit()` | `break` | `continue` | `return` | `pass` |
| 56   | 1    | `import` | 파이썬에서 외부 모듈을 불러오는 키워드는 무엇인가? | `from` | `import` | `include` | `require` | `using` |
| 57   | 2    | `3` | `print([1, 2, 3][2])`의 결과는 무엇인가? | | | | | |
| 58   | 3    | X    | 파이썬에서 `while` 문은 반드시 한 번은 실행된다. | | | | | |
| 59   | 1    | `del` | 파이썬에서 객체를 삭제하는 명령어는 무엇인가? | `remove` | `del` | `erase` | `clear` | `unset` |
| 60   | 2    | `6` | `x = 4; y = 2; print(x + y)`의 결과는 무엇인가? | | | | | |
| 61   | 1    | `continue` | 반복문에서 현재 반복을 건너뛰고 다음 반복을 실행하는 명령어는 무엇인가? | `break` | `continue` | `pass` | `exit` | `return` |
| 62   | 3    | O    | 파이썬에서는 변수 이름에 숫자가 앞에 올 수 없다. | | | | | |
| 63   | 2    | `True` | `bool(0)`은 무엇을 반환하는가? | | | | | |
| 64   | 1    | `lambda` | 파이썬에서 간단한 함수를 정의할 때 사용하는 키워드는 무엇인가? | `def` | `lambda` | `function` | `proc` | `method` |
| 65   | 3    | X    | 파이썬에서 함수는 일급 객체가 아니다. | | | | | |
| 66   | 1    | `sorted()` | 파이썬에서 리스트를 오름차순으로 정렬하는 함수는 무엇인가? | `sort()` | `order()` | `sorted()` | `arrange()` | `reverse()` |
| 67   | 2    | `False` | `bool([])`의 결과는 무엇인가? | | | | | |
| 68   | 3    | O    | 파이썬에서 `str` 자료형은 불변(immutable)이다. | | | | | |
| 69   | 1    | `None` | `x = [1, 2]; x.clear()` 후 `x`의 값은 무엇인가? | `[]` | `None` | `0` | `False` | `True` |
| 70   | 2    | `9` | `3 ** 2`의 결과는 무엇인가? | | | | | |
| 71   | 1    | `exec()` | 파이썬에서 문자열 형태의 코드를 실행하는 함수는 무엇인가? | `exec()` | `eval()` | `run()` | `compile()` | `eval()` |
| 72   | 2    | `5` | `print(int('5'))`의 결과는 무엇인가? | | | | | |
| 73   | 3    | X    | 파이썬에서 `list`는 수정 불가능한 자료형이다. | | | | | |
| 74   | 1    | `str.upper()` | 문자열을 모두 대문자로 변환하는 메서드는 무엇인가? | `str.upper()` | `str.lower()` | `str.capitalize()` | `str.title()` | `str.swapcase()` |
| 75   | 2    | `True` | `bool('')`은 무엇을 반환하는가? | | | | | |
| 76   | 1    | `join()` | 문자열을 리스트로 결합할 때 사용하는 함수는 무엇인가? | `split()` | `join()` | `concat()` | `add()` | `merge()` |
| 77   | 2    | `7` | `x = 7; y = 3; print(x % y)`의 결과는 무엇인가? | | | | | |
| 78   | 1    | `is` | 두 객체가 동일한 메모리 주소를 참조하는지 확인할 때 사용하는 연산자는 무엇인가? | `==` | `is` | `equals()` | `same()` | `==` |
| 79   | 3    | X    | 파이썬에서 `str`은 변경 가능한 자료형이다. | | | | | |
| 80   | 2    | `True` | `bool('False')`은 무엇을 반환하는가? | | | | | |
| 81   | 1    | `any()` | 파이썬에서 iterable 객체의 요소 중 하나라도 참이면 `True`를 반환하는 함수는 무엇인가? | `any()` | `all()` | `sum()` | `min()` | `max()` |
| 82   | 1    | `range()` | 주어진 범위에 대해 숫자 시퀀스를 생성하는 함수는 무엇인가? | `range()` | `list()` | `tuple()` | `dict()` | `set()` |
| 83   | 2    | `20` | `x = 10; print(x * 2)`의 결과는 무엇인가? | | | | | |
| 84   | 1    | `int()` | 문자열을 정수로 변환하는 함수는 무엇인가? | `str()` | `int()` | `float()` | `list()` | `bool()` |
| 85   | 3    | O    | 파이썬에서 두 개 이상의 값을 비교할 때 `and` 연산자를 사용할 수 있다. | | | | | |
| 86   | 2    | `True` | `bool([])`의 결과는 무엇인가? | | | | | |
| 87   | 1    | `sorted()` | 파이썬에서 리스트를 내림차순으로 정렬하려면 어떤 함수를 사용해야 하는가? | `sorted()` | `reverse()` | `sort()` | `reversed()` | `flip()` |
| 88   | 1    | `id()` | 파이썬에서 객체의 고유 아이디를 반환하는 함수는 무엇인가? | `id()` | `hash()` | `type()` | `object()` | `pointer()` |
| 89   | 3    | O    | 파이썬에서 `None`은 특수한 값으로, 값이 없음을 나타낸다. | | | | | |
| 90   | 2    | `6` | `x = [1, 2, 3, 4]; print(x[2] * 2)`의 결과는 무엇인가? | | | | | |
| 91   | 1    | `divmod()` | 두 숫자의 몫과 나머지를 튜플로 반환하는 함수는 무엇인가? | `mod()` | `divmod()` | `remainder()` | `div()` | `quotient()` |
| 92   | 3    | O    | 파이썬에서 `continue`는 반복문에서 남은 반복을 건너뛴다. | | | | | |
| 93   | 2    | `9` | `print(3 * 3)`의 결과는 무엇인가? | | | | | |
| 94   | 1    | `assert` | 파이썬에서 조건이 참이 아닐 경우 오류를 발생시키는 함수는 무엇인가? | `raise` | `assert` | `check` | `error` | `validate` |
| 95   | 2    | `5` | `x = 10; y = 5; print(x / y)`의 결과는 무엇인가? | | | | | |
| 96   | 3    | X    | 파이썬에서 `is`는 두 객체의 값을 비교한다. | | | | | |
| 97   | 2    | `0` | `x = 5; print(x - 5)`의 결과는 무엇인가? | | | | | |
| 98   | 1    | `filter()` | 특정 조건에 맞는 값만 필터링할 때 사용하는 함수는 무엇인가? | `filter()` | `map()` | `reduce()` | `zip()` | `sort()` |
| 99   | 2    | `8` | `x = 16; print(x // 2)`의 결과는 무엇인가? | | | | | |
| 100  | 1    | `enumerate()` | 리스트의 각 항목과 인덱스를 함께 반환하는 함수는 무엇인가? | `enumerate()` | `map()` | `zip()` | `range()` | `index()` |
